### bash (gzip a file)
gzip -c tiny-imagenet-200 > tiny-imagenet-200.gz

### Python (unzip a file)
from zipfile import ZipFile 

with ZipFile("imagenet-object-localization-challenge.zip", 'r') as zObject:
	zObject.extractall(path="ImageNet") 


